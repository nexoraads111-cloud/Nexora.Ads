(function(){const GAS=typeof NEXORA_GAS_URL!=='undefined'?NEXORA_GAS_URL:'';const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)];$('#menu')?.addEventListener('click',()=>$('.navlinks').classList.toggle('open'));$$('[data-order]').forEach(b=>b.onclick=()=>{$('#orderModal').classList.add('open');$('#plan').value=b.dataset.order||'Консультація'});$$('.close').forEach(b=>b.onclick=()=>b.closest('.modal').classList.remove('open'));function toast(t){const e=$('#toast');e.textContent=t;e.classList.add('on');setTimeout(()=>e.classList.remove('on'),3500)}async function gas(action,data){const body=JSON.stringify({action,...data});const r=await fetch(GAS+'?payload='+encodeURIComponent(body)+'&_='+Date.now(),{cache:'no-store',redirect:'follow'}),d=JSON.parse(await r.text());if(!d||d.ok===false||d.error)throw Error();return d}$$('.order-form').forEach(f=>f.onsubmit=async e=>{e.preventDefault();const b=$('button[type=submit]',f),o=b.textContent;b.disabled=true;b.textContent='Надсилаємо…';try{await gas('submitOrder',{name:$('[name=name]',f).value,contact:$('[name=contact]',f).value,plan:$('[name=plan]',f)?.value||'Консультація',message:$('[name=message]',f).value});f.reset();$('#orderModal')?.classList.remove('open');toast('✓ Заявку надіслано. Скоро зв’яжемося!')}catch(x){toast('Напишіть нам: nexora.ads111@gmail.com')}finally{b.disabled=false;b.textContent=o}});$('#reviewForm')?.addEventListener('submit',async e=>{e.preventDefault();const f=e.currentTarget;try{await gas('submitReview',{name:$('[name=name]',f).value,title:$('[name=title]',f).value,text:$('[name=text]',f).value,type:'Створення сайту',rating:+$('[name=rating]',f).value});f.reset();toast('✓ Дякуємо! Відгук з’явиться після перевірки.')}catch(x){toast('Спробуйте ще раз.')}});async function reviews(){const box=$('#liveReviews');if(!box||!GAS)return;try{const r=await fetch(GAS+'?action=reviews&_='+Date.now()),a=JSON.parse(await r.text());if(Array.isArray(a)&&a.length)box.innerHTML=a.map(x=>`<article class="quote"><div class="stars">${'★'.repeat(+x.rating||5)}</div><p>${String(x.text||'').replace(/</g,'&lt;')}</p><b>${String(x.name||'Клієнт').replace(/</g,'&lt;')}</b></article>`).join('')}catch(x){}}const io=new IntersectionObserver(es=>es.forEach(x=>{if(x.isIntersecting){x.target.classList.add('in');io.unobserve(x.target)}}));$$('.reveal').forEach(x=>io.observe(x));reviews()})();

